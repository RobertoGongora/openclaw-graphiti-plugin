## What does this PR do?

Brief description of the changes.

## How was it tested?

Describe how you verified the changes work.

## Checklist

- [ ] Python service: lint and tests pass (`uv run ruff check && uv run pytest -q`)
- [ ] Legacy OpenClaw plugin (only if TypeScript files changed): `npm test` and `npm run typecheck` pass
- [ ] CHANGELOG.md updated (if user-facing change)
- [ ] Documentation updated (if needed)
