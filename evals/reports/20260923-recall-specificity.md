# Recall specificity and source context, 23 September 2026

The baseline is `release-candidate` at `3f1ab72`, after PR184. Both versions
rank the same frozen facts. The Supabase projection has 166 facts at graph
revision 20612, captured at `2026-09-23T15:33:16.067756+00:00`. The desired
answer is the original source-reviewed record of automatic disk growth, not a
summary generated by this evaluation. This tests recall of recorded guidance,
not current Supabase policy.

| Question | Baseline answer rank | Candidate rank | Maximum permitted rank |
| --- | ---: | ---: | ---: |
| How does Supabase treat database disk size? | 4 | 3 | 3 |
| Expanded question including allocated/actual size, growth and shrinking | 9 | 8 | 8 |
| disk | 1 | 1 | 1 |

The first page contains eight results. The expanded query previously missed its
answer on that page. The original eight scored cases retain exactly their
baseline ranks: 2, 3, 3, 2, 1, 1, 1, 4. One original case remains unscored because
it has no source-reviewed expected answer. All 11 scored cases pass their rank
budgets; compact and question-based full detail select the same IDs in all 12
cases. There are no service-specific ranking rules. Synthetic committed tests
cover the natural Supabase question and similar database, framework and language
questions, alongside the private frozen corpus.

This is a modest ranking improvement. Exploratory reports still outrank the
disk-growth answer, and the expanded query only reaches position eight. The
lexical ranker does not establish which record best answers the question
semantically. Removing the schema bonus indiscriminately or adding length and
field weighting hurt other regression cases; neither is included. These are
development cases, not an independent accuracy estimate.

For the natural question, compact JSON grows from 4,112 to 4,207 bytes because
selection changes. Median formatting time is 2.32 ms before and 2.27 ms after
(101 repetitions). The expanded and short queries are also about 2.3 ms. Those
measurements exclude copying the frozen snapshot, database/network/MCP overhead,
and model calls; the small differences do not establish a speed improvement.

Evidence formatting was separately checked against the three original stored
episodes. The September planning note is labelled `shell_output` and shows the
recorded command reading guidance files. It is not attributed to one file in the
compound output. Both August documentation records are labelled
`documentation_lookup` from their recorded Context7 tool names. These are source
collection labels, not endorsements or fresh checks. Source context is fetched
on demand through `memory_evidence`, without increasing compact payloads with
commands or artifact contents.

The extraction engine fingerprint remains
`d3ee24a45005af4b14cffdc3a5e65e7aff9ea826f44c533900016c6d624b1b98`.
This change does not trigger re-extraction or change fact status.

Aggregate results, snapshot checksum and candidate retrieval-file checksum are
in [the JSON report](20260923-recall-specificity.json). Private inputs and
original evidence remain under ignored `.local/`. Re-run the fixed expectations
with:

```sh
uv run python -m evals.recall_regression \
  .local/recall-specificity-frozen-cases.json --output .local/recall-specificity-check.json
```

The command exits nonzero when a scored answer misses its maximum rank.
